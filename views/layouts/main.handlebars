<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Complete Stack</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
</head>

  <div class="wrapper">
    <header>
        <h1 class="has-text-link px-3 py-3 has-text-weight-bold">The Complete Stack: A Tech Blog</h1>
        <nav class="navbar mt-2">
            {{#if loggedIn}}
            <a href="/" class="has-text-link navbar-item is-hoverable pt-1">Homepage</a>
            <a href="/dashboard" class="navbar-item is-hoverable pt-1 has-text-link">Dashboard</a>
            <a href="/dashboard/create/" class="navbar-item is-hoverable pt-1 mr-4 has-text-link">Create post</a>
            <button id="logout" class="button is-danger">logout</button>
            {{else}}
            <a href="/" class="has-text-link navbar-item is-hoverable pt-1">Homepage</a>
            <a href="/login" class="button is-link">login</a>
            {{/if}}
        </nav>
    </header>

    <main>
            {{{ body }}}
    </main>

    <footer class="footer rows pb-2">
        <div class="row-is-full content has-text-centered">
            <p>
            <a href="/">Homepage</a>{{#if loggedIn}} | 
            <a href="/dashboard/">Dashboard</a> | 
            <a href="/dashboard/create/">Create post</a>
            {{else}}
             | <a href="/login/">Login</a>
            {{/if}}
            </p>
        </div>
    </footer>
  </div>

{{#if loggedIn}}
<script src="/javascript/logout.js"></script>
{{/if}}
</body>

</html>